import { Transition } from '../types';
export declare const eachValue: (map: {
    [key: string]: Transition;
}) => Transition;
export declare const fromPose: (map: {
    [key: string]: Transition;
}) => Transition;
