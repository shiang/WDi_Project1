import { Action } from 'popmotion/action';
import { Transition } from '../types';
export declare const just: (from: string | number) => Action;
declare const _default: Map<string, Transition>;
export default _default;
