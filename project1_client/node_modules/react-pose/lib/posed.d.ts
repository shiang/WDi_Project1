import { ReactElement } from 'react';
import { PoseElementProps } from './components/PoseElement.types';
import { PoserProps } from 'popmotion-pose';
export declare type ComponentFactory = (poseProps?: PoserProps) => (props: PoseElementProps) => ReactElement<any>;
export declare type Posed = {
    [key: string]: ComponentFactory;
};
declare const posed: Posed;
export default posed;
